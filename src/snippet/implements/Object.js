let onChain = {};
console.log(onChain.toString());
class NEWToString {
  toString() {
    return 'Ybibabo'
  }
}
let override = new NEWToString();
console.log(override.toString());

// let notOnChain = Object.create(null);
// console.log(notOnChain.toString());

let array = [];
console.log(array.toString());
